// =============================================================================
// Test: SYS_IPC_AWAIT — Compile-Only Verification
// =============================================================================
// Verifies that sys_ipc_await links correctly as a kernel syscall.
// This is a compile-only test — the syscall requires kernel scheduling.
// =============================================================================

extern fn sys_ipc_await() -> i64;

fn main() -> i32 {
    // Verify the symbol compiles and links.
    // In a real kernel context, this parks the process until IPC arrives.
    // We just verify the function signature is correct.
    let _result: i64 = 0;

    return 0;
}
