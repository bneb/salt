
struct MyStruct {
    arr: [i32; 5]
}

pub fn main(argc: i32) -> i32 {
    let vals: [i32; 5] = [1, 2, 3, 4, 5];
    
    // This struct initialization with array variable triggered the bug
    let s = MyStruct { arr: vals };
    
    if s.arr[0] != 1 {
        return 1;
    }
    return 0;
}
