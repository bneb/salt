global COUNTER: i32;

fn increment() {
    let mut val = COUNTER;
    val = val + 1;
    COUNTER = val;
}

fn main() -> i32 {
    COUNTER = 10;
    increment();
    if COUNTER == 11 {
        return 0;
    }
    return 1;
}
