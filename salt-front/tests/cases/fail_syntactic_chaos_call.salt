fn main() -> i32 {
    // Target emit_addr Call error (Assign to Call)
    // foo() = 5; 
    // Rust parser might reject this? "invalid left-hand side of assignment".
    // syn might parse it as Expr::Assign.
    foo() = 5;
    return 0;
}

fn foo() -> i32 { 0 }
