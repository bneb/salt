// Fail: Invalid promotion (multi-field struct to int)
struct Foo {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    let s = Foo { x: 42, y: 43 };
    let i: i32 = s; // ERROR: Multi-field struct cannot be promoted to int
    return 0;
}
