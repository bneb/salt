// Concept with complex binary expression
concept Complex<T> {
    requires(t: T) {
        t + 1 > 10
    }
}

fn test<T: Complex>(val: T) {
}

fn main() -> i32 {
    // Explicit specialization forces codegen for 'test' with T=i32
    test::<i32>(5);
    return 0;
}
