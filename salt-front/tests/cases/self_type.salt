struct Counter { count: i32 }

impl Counter {
    fn reset(self) {
        // self is Counter (owned)
        // This is a dummy method to test grammar
    }
}

fn main() {
    let mut c = Counter { count: 10 };
    c.reset();
}
