
struct Resource {
    id: i32
}

fn consume(r: Resource) {
    let _ = r.id;
}

fn main() -> i32 {
    let r1 = Resource { id: 42 };
    consume(r1); // r1 consumed here
    
    let x = r1.id; // Error: Use of moved value: r1. Consumed at line 12
    return x;
}
