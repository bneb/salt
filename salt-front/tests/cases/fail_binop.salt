// Fail: Binary op mismatch (multi-field struct)
struct Point { x: i32, y: i32 }
pub fn main() -> i32 {
    let p = Point { x: 1, y: 2 };
    let x = p + 1; // ERROR: Binary op not supported (multi-field struct)
    return 0;
}
