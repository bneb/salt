package kernel.core.pmm;

struct Resource { id: i32 }

fn take_resource(r: Resource) {
    // consumes r
}

fn return_nothing() {
    return;
}

fn main() {
    let r = Resource { id: 10 };
    take_resource(r);
    
    let mut x = 5;
    let px = &mut x;
    *px = 10;
    
    return_nothing();
}
