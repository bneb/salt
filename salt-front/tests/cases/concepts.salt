concept Positive {
    requires(x: i32) { x > 0 }
}

impl Positive<i32>;

fn safe_div(n: i32, d: i32) -> i32 
    requires { Positive(d) }
{
    return n / d;
}

fn main() {
    let x = safe_div(10, 2);
}
