package main

// Test 1: Ptr::empty() creates address-0 pointer
// Expected: prints "0" (the address of an empty pointer)

import std.core.ptr.Ptr

struct Node { val: i32 }

fn main() -> i32 {
    let p = Ptr::<Node>::empty();
    println(f"addr: {p.addr() as i64}");
    return 0;
}
