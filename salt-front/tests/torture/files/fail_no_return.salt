fn missing_return(x: i32) -> i32 {
    if x > 10 {
        return x;
    }
    // Missing return here - should hit "No Fall-through" logic or error
}

fn main() -> i32 {
    missing_return(5);
    return 0;
}
