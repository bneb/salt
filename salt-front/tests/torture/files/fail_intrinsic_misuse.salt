struct MyStruct {
    a: i32,
}

fn main() -> i32 {
    let s = MyStruct { a: 10 };
    
    // Calling undefined method/intrinsic on struct
    // Expecting "Unsupported Type" or similar error in codegen
    let x = s.popcount(); 
    
    return 0;
}
