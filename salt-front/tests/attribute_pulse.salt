package tests.attribute_pulse;

// This loop should have NO yield checks if compilations works correctly.

fn main(argc: i32) -> i32 {
    let mut sum = 0;
    
    for i in 0..1000 {
        sum += i;
    }
    
    return sum;
}
