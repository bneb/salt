// verification_fail.salt
// Expectation: Compilation fails with "Verification Failed"

fn safe_div(a: i32, b: i32) -> i32 requires(b != 0) {
    return a / b;
}

fn main() -> i32 {
    // This call passes 0 as the second argument, violating b != 0
    return safe_div(10, 0)
}
