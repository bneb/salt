// verification_pass.salt
// Expectation: Compilation succeeds

fn safe_div(a: i32, b: i32) -> i32 requires(b != 0) {
    return a / b;
}

fn main() -> i32 {
    // This call passes 55, satisfying b != 0
    return safe_div(10, 55)
}
